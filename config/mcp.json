{
  "mcpServers": {
    "tigergraph": {
      "command": "uvx",
      "args": ["tigergraph-mcp"],
      "env": {
        "TIGERGRAPH_HOST": "${TIGERGRAPH_HOST}",
        "TIGERGRAPH_GRAPH": "${TIGERGRAPH_GRAPH}",
        "TIGERGRAPH_TOKEN": "${TIGERGRAPH_TOKEN}"
      }
    }
  }
}

